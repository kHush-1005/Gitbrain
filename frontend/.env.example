# GitBrain Frontend Environment Variables
# Copy: copy .env.example .env  (Windows)
#       cp .env.example .env    (Mac/Linux)

# FastAPI backend URL — change if you deploy the backend elsewhere
VITE_API_BASE_URL=http://127.0.0.1:8000
